function resizeContainer()
{
    if(document.title == 'Home | Plant Tracker')
    {
        //1. Get height of page

        var pageHeight = document.documentElement.scrollHeight;
        console.log(pageHeight);

        //2. Get height of each plant rect
        //3. Get height of the spaces in between each plant rect
        //4. Make red container a bit taller than the height of all the plant rect + the spaces between them
    }    
}
